### 1. Clone the repository
```bash
git clone https://github.com/Sithole-Nhlamss/kirby.git
cd kirby

composer install/ pnpm install / npm install

pnpm run dev    # Start dev server with hot reload
pnpm run build  # Production build


php -S localhost:8000 -t public

Then open http://localhost:8000 and http://localhost:8000/panel in your browser
